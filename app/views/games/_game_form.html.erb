<%= bootstrap_form_for(@game) do |game| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this Game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message  %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<h2> Teams </h2>
<!-- Select home team -->
 <div class="field">
  <%= game.select :hometeam_id, Team.all.collect {|p| [p.name, p.id] } %>
 </div>
<!-- Select home team end -->

<!-- Select away team -->
 <div class="field">
  <%= game.select :awayteam_id, Team.all.collect {|p| [p.name, p.id] } %>
 </div>
<!-- Select away team end -->
<hr>
<h2> Home Team Stats </h2>

<!-- Begin home team scores -->
 <div class="field">
  <%= game.text_field :home_inningone, label: "Inning one score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningtwo, label: "Inning two score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningthree, label: "Inning three score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningfour, label: "Inning four score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningfive, label: "Inning five score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningsix, label: "Inning six score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningseven, label: "Inning seven score" %>
 </div>
  
 <div class="field">
  <%= game.text_field :home_inningeight, label: "Inning eight score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_inningnine, label: "Inning nine score" %>
 </div>

 <div class="field">
  <%= game.text_field :home_runs, label: "Runs" %>
 </div>

 <div class="field">
  <%= game.text_field :home_hits, label: "Hits" %>
 </div>

 <div class="field">
  <%= game.text_field :home_error, label: "Errors" %>
 </div>

<!-- Home team scores end -->

<hr>
<h2> Away Team Stats </h2>

<!-- Begin Away team scores -->
 <div class="field">
  <%= game.text_field :away_inningone, label: "Inning one score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningtwo, label: "Inning two score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningthree, label: "Inning three score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningfour, label: "Inning four score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningfive, label: "Inning five score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningsix, label: "Inning six score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningseven, label: "Inning seven score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningeight, label: "Inning eight score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_inningnine, label: "Inning nine score" %>
 </div>

 <div class="field">
  <%= game.text_field :away_runs, label: "Runs" %>
 </div>

 <div class="field">
  <%= game.text_field :away_hits, label: "Hits" %>
 </div>

 <div class="field">
  <%= game.text_field :away_error, label: "Errors" %>
 </div>

<!-- Away team scores end -->

<!-- Submit -->
 <div class = "actions">
  <%= game.submit %>
 </div>
<!-- End submit -->

 <% end %>
